Start the Bitcoin Journey dev server.

Run this command in the background and confirm when it's ready:

```bash
cd /Users/ocean/code/paucotan/archive/personal_projects/bitcoin-journey && npm run dev
```

Tell the user the server is running at http://localhost:5173
